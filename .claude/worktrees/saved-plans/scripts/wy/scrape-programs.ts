/**
 * scrape-programs.ts — degree/program requirements for WY.
 *
 * Auto-generated by scripts/lib/discover-programs.ts. Each entry maps a
 * college slug to the catalog platform discovered for that college. Edit
 * configs (catoid, catalogYear, programNavoids, etc.) per the platform
 * template's options interface before relying on the data.
 *
 * Usage:
 *   npx tsx scripts/wy/scrape-programs.ts
 */

import * as fs from "fs";
import * as path from "path";
import { applyProgramMatching } from "../../lib/programs/matcher.js";
import { scrapeCourseleafPrograms } from "../lib/scrape-courseleaf-programs.js";
import { scrapeAcalogPrograms } from "../lib/scrape-acalog-programs.js";

async function run(
  slug: string,
  scrape: () => Promise<{ programs: unknown[]; catalog_year: string; catalog_url: string; college_slug: string; scraped_at: string }>,
): Promise<void> {
  console.log(`\n=== ${slug} ===`);
  try {
    const data = await scrape();
    if (data.programs.length === 0) {
      console.log(`  No programs found for ${slug}.`);
      return;
    }
    const { matched, unmatched } = applyProgramMatching(data.programs as never);
    console.log(`  Matcher: ${matched} matched / ${unmatched} unmatched`);
    const outDir = path.join(process.cwd(), "data", "wy", "programs");
    fs.mkdirSync(outDir, { recursive: true });
    const outPath = path.join(outDir, `${slug}.json`);
    fs.writeFileSync(outPath, JSON.stringify(data, null, 2));
    console.log(`  ✓ Wrote ${data.programs.length} programs → ${outPath}`);
  } catch (e) {
    console.error(`  ✗ ${slug} failed: ${e}`);
  }
}

async function main() {
  console.log("WY program scraper");
  // western-wyoming-community-college (courseleaf)
  await run("western-wyoming-community-college", () =>
    scrapeCourseleafPrograms({ collegeSlug: "western-wyoming-community-college", baseUrl: "https://catalog.western.edu" }),
  );

  // eastern-wyoming-college (courseleaf)
  await run("eastern-wyoming-college", () =>
    scrapeCourseleafPrograms({ collegeSlug: "eastern-wyoming-college", baseUrl: "https://catalog.eastern.edu" }),
  );

  // northern-wyoming-community-college-district (acalog)
  await run("northern-wyoming-community-college-district", () =>
    scrapeAcalogPrograms({ collegeSlug: "northern-wyoming-community-college-district", baseUrl: "https://catalog.northern.edu", catoidFallback: 0, programNavoids: [], autoDiscoverCatoid: true }),
  );
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
